name: Ruff Code Quality Check

on:
  pull_request:
    branches: [ main ]
    types: [opened, synchronize, reopened]

jobs:
  ruff-check:
    runs-on: ubuntu-latest
    environment: ruff-check  # This requires approval before running
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6
      
    - name: Install uv
      uses: astral-sh/setup-uv@v7
      with:
        version: "latest"
        
    - name: Set up Python
      run: uv python install 3.13
        
    - name: Install dependencies
      run: uv sync
        
    - name: Run Ruff linting
      run: uv run ruff check customizations/ custom_server.py tests/customizations/ --output-format=github
        
    - name: Run Ruff formatting check
      run: uv run ruff format --check customizations/ custom_server.py tests/customizations/